
<br>

### Docker create and run a new container from an image

* What is the **content** of the file `/tmp/content.txt` embedded in `dejanualex/secret_image:1.0 ` image? Write your response in the existing file `/tmp/two`{{copy}}